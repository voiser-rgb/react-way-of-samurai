import React from 'react';
import styles from './Dialogs.module.css';

const Dialogs = (props) => {
	return (
	<div>
		<span className={styles.text}>Dialogs</span>
	</div>
	);
}

export default Dialogs;